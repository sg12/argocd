apiVersion: v1
kind: Service
metadata:
  name: redis-svc # Это имя будет хостом для подключения
spec:
  type: ClusterIP # Доступен только внутри кластера
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
